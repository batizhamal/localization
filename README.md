# localization

## Что есть на данный момент:

- Загрузка ru.json (при обновлении страницы не пропадают данные, записываются в localStorage при загрузке файлов)
- Parsing of nested objects (берет ru.json и вытаскивает ключи)
- Загрузка kz.json (при обновлении страницы не пропадают данные, записываются в localStorage при загрузке файлов)
- Дельта между ru.json и kz.json
- Редактировать ru.json (при обновлении не пропадают изменения, записываются в localStorage)
- Скачать ru.json
- Редактировать kz.json (при обновлении не пропадают изменения, записываются в localStorage)
- Скачать kz.json

## Current problems:

- Дельта между массивами в объектах - показыватся неправильно

## Past problems (решенные):

- Решен нюанс грязного кода касательно upload и download, вынесением методов для загрузки и скачивания файлов в отдельный "DataService.js"
- Решен нюанс с парсингом объекта с вложенностями и получения ключей
- Решен нюанс редактирования kz.json (чтобы вложенные уровни правильно сохранялись)
- Решен нюанс с загрузкой kz.json и с дельтой (не включая нюанс выше)
