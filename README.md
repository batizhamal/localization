# localization

## Что есть на данный момент:

- Можно загрузить ru.json (при обновлении страницы не пропадают данные, записываются в localStorage при загрузке файлов)
- Работает parsing of nested objects (берет ru.json и вытаскивает ключи)
- Можно загрузить kz.json (при обновлении страницы не пропадают данные, записываются в localStorage при загрузке файлов)
- Если есть delta, отображается пустыми полями
- Можно редактировать ru.json (при обновлении не пропадают изменения, записываются в localStorage)
- Можно скачать ru.json
- (Можно скачать kz.json)

## В разработке:

- Редактирование kz.json (чтобы вложенные уровни правильно сохранялись)

## Past problems:

- Считаю, что код грязный (работаю над другими способами upload и download)
-> Нюанс решен, вынесением методов для загрузки и скачивания файлов в отдельный "DataService.js"
- Пока что работает только с json без вложенностей
-> Нюанс решен, добавлен метод для парсинга объекта с вложенностями и получения ключей

## Current problems:

- Ошибка при редактировании kz.json (из-за вложенностей. Сейчас в разработке)
